# 05 — i18n (react-i18next)

Языки: **ru** (по умолчанию), **en**, **tj**. Все строки UI — через `t('...')`. Хардкод запрещён.

## Настройка → `src/i18n/index.ts`

```ts
import i18n from 'i18next'
import { initReactI18next } from 'react-i18next'
import LanguageDetector from 'i18next-browser-languagedetector'
import ru from './locales/ru.json'
import en from './locales/en.json'
import tj from './locales/tj.json'

i18n
  .use(LanguageDetector)        // читает localStorage 'i18nextLng'
  .use(initReactI18next)
  .init({
    resources: { ru: { translation: ru }, en: { translation: en }, tj: { translation: tj } },
    fallbackLng: import.meta.env.VITE_DEFAULT_LANG ?? 'ru',
    interpolation: { escapeValue: false },
  })

export default i18n
```

Импортировать `@/i18n` в `main.tsx` до рендера.

## Структура ключей (namespace через точку)

```jsonc
{
  "common":  { "addToCart": "...", "buyNow": "...", "viewAll": "...", "search": "..." },
  "nav":     { "home": "...", "contact": "...", "about": "...", "signUp": "...", "wishlist": "...", "cart": "...", "account": "..." },
  "auth":    { "createAccount": "...", "login": "...", "email": "...", "password": "...", "forgot": "...", "haveAccount": "..." },
  "home":    { "flashSales": "...", "browseByCategory": "...", "bestSelling": "...", "explore": "...", "newArrival": "..." },
  "product": { "inStock": "...", "outOfStock": "...", "relatedItems": "...", "color": "...", "size": "...", "quantity": "..." },
  "cart":    { "subtotal": "...", "total": "...", "shipping": "...", "checkout": "...", "couponCode": "...", "applyCoupon": "...", "returnToShop": "..." },
  "checkout":{ "billingDetails": "...", "placeOrder": "...", "firstName": "...", "streetAddress": "...", "townCity": "...", "phone": "..." },
  "profile": { "manageAccount": "...", "myProfile": "...", "addressBook": "...", "myOrders": "...", "editProfile": "...", "save": "..." },
  "about":   { "ourStory": "...", "teamRole": "..." },
  "contact": { "callToUs": "...", "writeToUs": "...", "sendMessage": "..." },
  "empty":   { "cart": "...", "wishlist": "...", "noProducts": "..." },
  "errors":  { "network": "...", "notFound": "...", "required": "...", "invalidEmail": "..." }
}
```

Создать три файла `ru.json`, `en.json`, `tj.json` с одинаковыми ключами. tj — таджикский (кириллица).

## Использование

```tsx
const { t } = useTranslation()
<button>{t('common.addToCart')}</button>
```

Переключение языка делает `features/lang-switcher` через `i18n.changeLanguage(code)` — без перезагрузки страницы.
